[project]
name = "portkey-rhoai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "llama-stack-client>=0.3.5",
    "portkey-ai>=2.1.0",
    "python-dotenv>=1.2.1",
    "redis>=7.1.0",
    "tabulate>=0.9.0",
]
